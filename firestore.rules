rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Voortgang per Firebase Auth uid (oefen/tentamen/chat-sync)
    match /userProgress/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
